## Symptom  
Replication conflicts and quorum failures in the kv-store, with all errors concentrated on node `kv-a1`, leading to version divergence between terms 41 and 42.  

## Timeline  
- **2026-08-06T13:05:00.138Z**: `cluster-net link degraded` between rack-a and rack-b (34% packet loss).  
- **2026-08-06T13:13:35.121Z**: `kv-store` on `kv-a1` continues committing writes with term 41.  
- **2026-08-06T13:13:36.975Z**: First replication conflict error (term 41 vs 42).  
- **2026-08-06T13:13:37.392Z**: `kv-a1` cannot reach quorum peers in rack-b, switches to local quorum.  
- **2026-08-06T13:13:43.405Z**: Final replication conflict error (term 41 vs 42).  

## Root cause hypothesis  
**Cluster network link degradation between rack-a and rack-b** (high confidence). The 34% packet loss likely caused `kv-a1` to lose connectivity to rack-b peers, forcing it to use local quorum. This led to term divergence (term 41 vs 42) and replication conflicts, as evidenced by:  
- `"cluster-net link degraded between rack-a and rack-b packet_loss=34%"` (line 1)  
- `"kv-store node=kv-a1 cannot reach quorum peers in rack-b"` (line 2026-08-06T13:13:37.392Z)  
- `"replication conflict key=orders/<n> versions diverged (term 41 vs 42)"` (multiple lines in peak window).  

## Ruled out  
- **Pre-existing kv-store replication conflicts**: These were present before the incident and occurred on the same node (`kv-a1`), but no evidence links them to the current event.  
- **Baseline quorum warnings**: The "cannot reach quorum peers in rack-b" warning was already present in the log (line 3), but the 34% packet loss was a new, rare event.  

## Suggested next steps  
1. **Investigate the cluster-net link degradation**: Check for recent changes to network hardware, routing, or firewall rules affecting rack-a/rack-b.  
2. **Validate quorum configuration**: Confirm `quorum_mode=available` is intentional and does not expose nodes to term divergence risks.  
3. **Audit kv-a1’s replication logs**: Identify if term 42 was generated by `kv-b2` (which remains error-free) and why `kv-a1` failed to sync.  
4. **Test network resilience**: Simulate rack-a/rack-b link failures to ensure quorum and replication recovery processes work as expected.  
5. **Monitor for recurring packet loss**: Deploy alerts for packet loss >10% on critical inter-rack links.
